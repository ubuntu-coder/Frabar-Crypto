import { NotFound } from "../components/common/notFound/NotFound";

function NotFoundPage() {
  return (
    <div className="min-w-[100vw] min-h-[90vh] flex items-center justify-center bg-white ">
      <NotFound />;
    </div>
  );
}

export default NotFoundPage;
